import 'package:media_kit_video/media_kit_video.dart';

void main() {
  // We can't use mirrors directly in flutter but we can print some definitions or test if classes compile.
  // Let's write a helper script to test if various names compile!
}
